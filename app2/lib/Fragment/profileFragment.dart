import 'package:flutter/cupertino.dart';

class profileFragment extends StatelessWidget{


  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('profileFragment'),
      ),
    );
  }

}